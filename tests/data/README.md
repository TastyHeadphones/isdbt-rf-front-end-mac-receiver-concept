# Test Data

Store only legal or synthetic data in this directory.

- `synthetic_demo.ts` is generated locally by tooling and not committed by default.
- Do not commit protected or unauthorized broadcast content.
