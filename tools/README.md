# Tooling

This directory contains helper scripts for Lab Demo Mode and validation automation.

## Scripts

- `generate_synthetic_ts.py`: generates deterministic TS-like packet streams for legal lab replay
- `lab_demo_smoke.py`: optional smoke runner for backend discovery/tune/replay checks

## Python Setup

```bash
python3 -m pip install -r tools/requirements.txt
```
