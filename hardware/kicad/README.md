# KiCad Project Area

KiCad 8 style structure is prepared for schematic and PCB evolution. This folder currently contains concept planning artifacts and placeholder conventions.

When implementation starts:

- Add `.kicad_pro`, `.kicad_sch`, `.kicad_pcb` files.
- Split hierarchical sheets by subsystem (RF front-end, power, bridge, debug).
- Keep symbol/footprint libraries version-pinned.
